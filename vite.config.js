import { defineConfig } from 'vite'

export default defineConfig({
    // ВАЖЛИВО: Замініть '/REPO_NAME/' на назву вашого репозиторію на GitHub
    // Наприклад, якщо репозиторій називається 'my-portfolio', то пишіть '/my-portfolio/'
    // Якщо ви деплоїте на кореневий домен (username.github.io), залиште '/'
    base: '/myportfolio/',
})
